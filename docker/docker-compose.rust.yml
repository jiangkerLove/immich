#
# Moved: all-in-one rust stack lives under rust-server/
#
#   cd ../rust-server
#   cp example.env .env
#   docker compose up -d --build
#
# See rust-server/README.docker.md
include:
  - path: ../rust-server/docker-compose.yml
