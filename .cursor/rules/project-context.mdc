---
description: Immich fork 项目背景与 rust-server 迁移总览
alwaysApply: true
---

# 项目背景（jiangkerLove/immich）

本仓库是 **Immich 的 fork**，正在将上游 TypeScript `server/` 迁移到 **`rust-server/`**（Rust/Axum）。
目标：尽可能对齐上游行为，后期**自行维护可正常使用的版本**。**不要**向上游 Immich 提 PR。

## 分支（仅两条长期分支）

| 分支 | 用途 |
|------|------|
| `main` | 同步上游 Immich |
| `dev-rust` | Rust 开发主线（用户说的 rust-dev） |

所有 rust 迁移 PR 的 base 必须是 **`dev-rust`**。临时分支：`cursor/<kebab-name>-4063`，合并后删除。

## 代码地图

```
server/src/          → 上游 TS 参考实现（对齐行为时先读这里）
rust-server/src/
  routes/            → HTTP 路由（≈ server/controllers）
  handlers/          → 请求处理
  service/           → 业务逻辑（≈ server/services）
  service/workers/   → BullMQ 消费者（≈ @OnJob）
  service/media/     → 媒体流水线
  models/db/         → SQL（≈ repositories）
```

## 迁移进度（摘要）

| 维度 | 状态 |
|------|------|
| HTTP API（全领域） | ✅ 已完成（~45 TS controller → ~42 Rust route 模块） |
| 66 个 JobName / 19 个队列 worker | ✅ 已完成（`search` 为遗留空队列 no-op） |
| 媒体 / 库 / 同步 / 搜索 API | ✅ 主体完成 |
| WebSocket + HLS Redis + ConfigUpdate/AppRestart | ✅ 已完成 |
| 工作流执行 | ✅ 仅 AssetV1（与上游一致） |
| sqlx baseline + immich-admin CLI | ✅ 已完成 |
| 单机可部署 | ✅ 默认单进程；overlay：`docker/docker-compose.rust.yml` |

## 优先待办（下一阶段 = 无缝切流）

1. ~~P0–P3 代码缺口（伙伴媒体 / album WS / HLS Redis / baseline / telemetry / logging / CLI）~~ — 已完成
2. **Cutover C1**：真实 compose 冒烟（`scripts/smoke.ps1` + 保留 ML 容器）
3. **Cutover C2**：现有库 `migration-status` / `schema-check`，确认无 `kysely_ahead_of_lock`
4. **Cutover C3**：维护模式 AppRestart 后进程管理器能重启
5. **P4 实测**（可选加深）：Search / Sync / ML QueueAll / Integrity / 伙伴共享 E2E
6. **baseline 锁定后**合上游：再写 `migrations/2+` 并更新 lock

## 详细文档

规划、已完成模块、差距清单、切流路线图见：**`.cursor/docs/migration-checklist.md`**

新会话做迁移/规划时，应先阅读该文件。
